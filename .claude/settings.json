{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "uv run ruff check src tests && uv run ruff format --check . && uv run mypy src && uv run pytest -q"
          }
        ]
      }
    ]
  }
}
